<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jar-plugin</artifactId>
            <version>3.4.1</version>
            <executions>
                <execution>
                    <id>default-jar</id>
                    <phase>package</phase>
                    <goals>
                        <goal>jar</goal>
                    </goals>
                    <!-- Only needed if producing a second JAR -->
                    <!-- <configuration> -->
                    <!--     <classifier>my-extra</classifier> -->
                    <!-- </configuration> -->
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
